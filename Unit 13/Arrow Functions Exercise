// ES2015 Arrow Functions Shorthand
const double = arr => arr.map(val => val * 2)

// Refactor the following function to use arrow functions:
const squareAndFindEvens = numbers => numbers.map(i => i ** 2).filter(a => a % 2 === 0)
